on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [14, 15, 16]
        package: ["${{ github.repository }}", "github:${{ github.repository }}", "https://github.com/${{ github.repository }}", "git+https://github.com/${{ github.repository }}", "git://github.com/${{ github.repository }}.git"]

    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - run: npm version

      - run: npm init -y

      - run: npm i ${{ matrix.package }} --save

      - run: cat package.json
